{
  "name": "haromszog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx lite-server",
    "build:src": "npx copyfiles -u 1 src/**/*.{html,js} dist",
    "build:bs": "npx copyfiles -u 4 node_modules/bootstrap/dist/css/bootstrap.css dist",
    "build": "npm run build:src & npm run build:bs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
